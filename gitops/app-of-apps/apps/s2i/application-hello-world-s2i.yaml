apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: hello-world-s2i
  namespace: openshift-gitops
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: hello-world-s2i
  project: default
  source:
    directory:
      recurse: true
    path: manifests/s2i
    repoURL: 'https://github.com/shirodkar/helloworld-ear.git'
    targetRevision: HEAD
  syncPolicy:
    automated:
      enabled: true
      prune: true
      selfHeal: true